# Registro de Resultados de Estrategias

## Criterios objetivo
- Profit Factor > 1.5
- Max Drawdown < 10%
- Sharpe > 1.0

| Intento | Activo | Temporalidad | Estrategia | Profit Factor | Max Drawdown | Sharpe | Veredicto y Proximo Paso |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 001 | N/A | N/A | Inicializacion del registro | N/A | N/A | N/A | Registro creado. Listo para primeras corridas reales. |
| 002 | own-ide / agent runtime | N/A | Fase 1B agent-first: continuidad y contexto dentro del runtime propio | N/A | N/A | N/A | Veredicto: mejora estructural validada. `freejt7-agent-runtime` ya arma el contexto conversacional y `provider-router` delega la ruta agente. Proximo paso: Fase 1C para centralizar decision operativa, herramientas y persistencia de sesion. |
| 003 | own-ide / agent runtime | N/A | Fase 1C agent-first: planificacion operativa por tarea y persistencia de routePlan | N/A | N/A | N/A | Veredicto: mejora estructural validada. El runtime ya decide la ruta por tarea y el panel persiste `routePlan`. Proximo paso: Fase 2 para mover seleccion de tools/MCP y mas estado de sesion al runtime propio. |
| 004 | own-ide / agent runtime | N/A | Fase 2 inicial: capabilityPlan, skills y MCP dentro del runtime propio | N/A | N/A | N/A | Veredicto: mejora estructural validada. El runtime ya publica `capabilityPlan` y el panel lo refleja. Proximo paso: mover el despacho real de tools/MCP al runtime propio. |
| 005 | own-ide / agent runtime | N/A | Fase 2 siguiente: despacho real de acciones locales desde el runtime propio | N/A | N/A | N/A | Veredicto: mejora estructural validada. El runtime ya no solo planifica; ahora prepara `actions` locales reales para el fallback local. Proximo paso: extender el mismo ownership a skills/MCP/harness. |
| 006 | own-ide / agent runtime | N/A | Fase 3 inicial: continuity via session agent state | N/A | N/A | N/A | Veredicto: mejora estructural validada. `continua` ya puede apoyarse en `agentState` persistido por sesion. Proximo paso: ampliar el estado operativo de sesion y la reanudacion del trabajo tras reinicio. |
| 007 | own-ide / control panel | N/A | Fase 2 cierre formal: panel chat-first con inspector lateral agent-first | N/A | N/A | N/A | Veredicto: Fase 2 cerrada. `control-panel.js` deja tareas como tab lateral por defecto, baja el tono de router/control-plane en la superficie principal y conserva la trazabilidad tecnica en el inspector. Proximo paso: cerrar formalmente Fase 3 con reanudacion real post-restart. |
| 008 | own-ide / agent runtime | N/A | Fase 3 formal: reconstruccion de `agentState`, `yield/resume` y continuidad post-restart | N/A | N/A | N/A | Veredicto: Fase 3 cerrada. `session-engine` ya reconstruye continuidad desde tareas persistidas, actualiza el estado al recuperar tareas interrumpidas y mantiene reanudacion verificable. Proximo paso: Fase 5/Fase 6 formales. |
| 008 | own-ide / copilot legacy | N/A | Fase 6 cierre formal: aislamiento de Copilot como ruta secundaria legacy | N/A | N/A | N/A | Veredicto: Fase 6 cerrada. `copilot_router.runtime` ya usa seleccion/config separada del provider principal, deja metadata explícita de compatibilidad secundaria y conserva override explícito compatible. Proximo paso: cerrar Fase 5 para terminar de subordinar providers y OpenClaw al runtime propio. |
| 008 | own-ide / agent runtime | N/A | Fase 4 cierre formal: dispatch provider-independent de skills, MCP y tools nativos | N/A | N/A | N/A | Veredicto: Fase 4 cerrada. `freejt7-agent-runtime` ahora publica `capabilityPlan.dispatch` con `owner`, `dispatchTarget` y `trace`, y `local-agent-runtime` preserva esa evidencia en resumen tecnico/verificacion. Proximo paso: cerrar Fase 5 subordinando providers/OpenClaw sin perder esta trazabilidad. |
| 009 | own-ide / panel + runtime | N/A | Pase integral final Manus: panel en settings del IDE, runtime con capacidades visibles y verificacion real en VSIX instalada | N/A | N/A | N/A | Veredicto: cierre integrador validado. El panel ya actua como frontend agent-first sobre ajustes del IDE, el prompt/runtime expone capacidades y continuidad con menos degradacion conversacional, y la VSIX 4.2.11 quedo empaquetada, reinstalada y probada en `own-ide`. Proximo paso: optimizar tamano del paquete sin tocar el contrato funcional cerrado. |
| 010 | own-ide / provider fallback | N/A | Respaldo multi-provider para fallos de OpenClaw o cuota del provider primario | N/A | N/A | N/A | Veredicto: mejora operativa validada. `extension.runtime` ya prepara fallback providers automáticos y el fallback directo cambia de provider cuando el primario falla, dejando `fallbackAttempts` trazado. Proximo paso: auto-curar también conflictos de gateway/puerto cuando haya evidencia fresca de choque en OpenClaw. |
| 011 | own-ide / provider catalog UI | N/A | Catalogo global de providers/modelos derivado del registro real | N/A | N/A | N/A | Veredicto: mejora de consistencia validada. `control-panel` y los QuickPick de configuración ya derivan providers/modelos del registro global y no de arrays duplicados, con refresh de catálogo para todos los providers. Proximo paso: subir también los enums del manifest a una estrategia más mantenible si el número de providers sigue creciendo. |
| 012 | own-ide / VSIX packaging | N/A | Verificacion bundle instalado vs bundle del repo para DeepSeek | N/A | N/A | N/A | Veredicto: causa raíz confirmada. El repo tenía el fix, pero la VSIX instalada en `own-ide` seguía stale. Reempaquetar con `package:local`, reinstalar y limpiar `workspaceStorage` cerró el gap entre código y extensión efectiva. Proximo paso: subir versión o automatizar hash/timestamp de VSIX para evitar reinstalar artefactos viejos con el mismo número. |
| 013 | own-ide / local-first diagnostics | N/A | Diagnostico local prioritario para preguntas sobre providers/modelos que no aparecen en la interfaz | N/A | N/A | N/A | Veredicto: mejora operativa validada. `extension.runtime` ahora prioriza la ruta local para diagnósticos de catálogo/provider y `local-agent-runtime` responde con evidencia concreta del repo/perfil en vez de caer a chat básico o auditoría genérica. Proximo paso: extender la misma política a otros autodiagnósticos del control-plane si aparecen más casos similares. |
| 014 | own-ide / agent protagonist redesign | N/A | Rediseño por 7 etapas para que Free JT7 actúe como protagonista del IDE propio | N/A | N/A | N/A | Veredicto: cierre estructural validado. `own-ide` abre en modo Free JT7, el agente expone autoridad única de configuración, los autodiagnósticos clave van local-first, la UI reduce fugas de backend, la policy autónoma de `coding` deja de pedir permiso interno y la VSIX final quedó reinstalada y probada. Proximo paso: endurecer aún más la autocuración del harness OpenClaw cuando falle el backend principal. |
| 015 | own-ide / provider nvidia | N/A | Alta global de NVIDIA con API keys por modelo | N/A | N/A | N/A | Veredicto: mejora estructural validada. `nvidia` quedó integrado en el registro global, en Settings/panel/OpenClaw y con autenticación por modelo en `SecretStorage` y fallback compatible por proveedor. Proximo paso: si aparecen más backends con este patrón, mover `apiKeyMode` y presets por modelo a un descriptor más declarativo. |
| 016 | own-ide / agent runtime + openclaw | N/A | Separar planificacion del goal real y payload serializado, y endurecer deteccion de OpenClaw en el perfil propio | N/A | N/A | N/A | Veredicto: causa raiz corregida. El runtime propio ya planifica con el objetivo del usuario y manda el prompt enriquecido solo al backend subordinado, evitando caer a `local-agent` por texto incidental del contexto serializado. Además `findOpenClawBinary` ya encuentra `~/.local/bin/openclaw`, lo que reduce dependencia del PATH del host gráfico. Proximo paso: si reaparecen respuestas básicas, revisar primero keys reales del provider y no el router del agente. |
| 017 | own-ide / backend propio Free JT7 | N/A | Convertir `freejt7` en backend primario y dejar OpenClaw subordinado | N/A | N/A | N/A | Veredicto: mejora estructural validada. `provider-router`, `session-engine`, `control-panel`, `bootstrap` y `freejt7-agent-runtime` ya entienden `runtimeBackend=freejt7`; `own-ide` queda sembrado con ese backend y la degradación local tiene más manos (`delete` y `exec`) para no colapsar en auditoría básica. Proximo paso: extraer un loop de herramientas más rico dentro del backend propio para reducir también la dependencia del provider directo. |
| 018 | own-ide / backend agentic propio | N/A | Sustituir `provider-direct` por un loop agentic real dentro de `freejt7-owned-runtime` | N/A | N/A | N/A | Veredicto: mejora estructural validada. El backend propio ahora usa planner JSON, ejecuta tools locales, verifica resultados y repregunta al planner antes de cerrar. Ya no depende de OpenClaw ni de una respuesta textual única para operar. Proximo paso: ampliar el mismo loop con edición incremental, MCP y skills para tareas multi-superficie. |
| 019 | own-ide / freejt7-agent-core-v2 | N/A | Backend profesional paralelo con registry de tools, rechazo de completed sin evidencia, config_patch, JSONL y migracion de own-ide a `freejt7-v2` | N/A | N/A | N/A | Veredicto: mejora funcional validada. Core-v2 ejecutó una prueba live creando/modificando/leyendo/verificando `copilot-agent/runs/core-v2-live/validation.txt`, persistió `copilot-agent/core-v2-runs.jsonl`, pasó smokes focalizados, build, package e instalación en own-ide. Proximo paso: ampliar dispatch MCP/skills y subagentes nativos dentro del mismo core-v2. |
| 020 | own-ide / core-v2 + bootstrap | N/A | Subagentes nativos en `core-v2` y verificacion del bundle real en `own-ide` | N/A | N/A | N/A | Veredicto: mejora funcional validada con matiz arquitectonico. `core-v2` ya delega subtareas a hijos nativos (`subagent_run`) con traza/evidencia y `own-ide` instala el bundle correcto aun cuando VSCodium bloquee reinstalar la misma VSIX, gracias al purge + fallback manual. Proximo paso: construir un E2E headless del panel/webview instalado y decidir si Free JT7 seguira siendo VSIX sobre VSCodium o pasara a shell/binario propio para sostener un claim fuerte tipo Manus/Codex. |
